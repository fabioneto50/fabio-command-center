name: Legacy medication-v11-14-check (retired)
on: workflow_dispatch
permissions:
  contents: read
jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This legacy workflow is retired. Use Quality gate 1.4 for current source, browser, data and security tests.'
