name: Earlier audit source preparation (archived)
on: workflow_dispatch
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Earlier preparation is archived. Current verification uses Quality gate 1.4.'
